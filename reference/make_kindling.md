# Register kindling engines with parsnip

This function registers the kindling engine for MLP and RNN models with
parsnip. It should be called when the package is loaded.

## Usage

``` r
make_kindling()
```
